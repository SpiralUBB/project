import { NumberValueAccessor } from "@angular/forms";

export interface Invitation {
    attendStatus: number,
    attendStatusText: string,
    id: string,
    status: number,
    statusText: string
}